/**
 * Avatar config — switch between Theo and Missi.
 *
 * Set USE_THEO = true  → Theo (boy, curly hair, t-shirt + jeans) — DEFAULT
 * Set USE_THEO = false → Missi (girl, long hair, pink glasses, pink frock)
 *
 * Change locally to experiment. Committed default is true (Theo).
 */

export const USE_THEO = true
