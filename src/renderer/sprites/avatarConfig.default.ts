/**
 * Avatar config — DEFAULT for builds and CI.
 *
 * Copy this file as avatarConfig.ts to override locally:
 *   cp avatarConfig.default.ts avatarConfig.ts
 *
 * Set USE_THEO = true  → Theo (boy, curly hair, t-shirt + jeans)
 * Set USE_THEO = false → Missi (girl, long hair, pink glasses, pink frock)
 */

export const USE_THEO = true
